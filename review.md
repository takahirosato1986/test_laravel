# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か

Modelインスタンスに紐づくテーブルのレコード全件取得

### Todoモデルのallメソッドの返り値は何か

Illuminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは

配列操作に特化しているので配列を直接操作するよりも簡単に操作ができる

### view関数の第1・第2引数の指定と何をしているか

第1引数で表示するファイル指定、第2引数でファイルに渡したいデータを指定し、
第1引数に指定したファイルのHTML分を表示する

### index.blade.phpの$todos・$todoに代入されているものは何か

// $todos = Collectionインスタンス
// $todo = Collectionインスタンス内のTodoインスタンス

## Todo作成機能

### Requestクラスのallメソッドは何をしているか

フォームで送信された値を一括取得

### fillメソッドは何をしているか

Todoインスタンスのプロパティへの一括代入

### $fillableは何のために設定しているか

一括代入で悪意のある操作をされないように代入するプロパティを設定している

### saveメソッドで実行しているSQLは何か

プロパティに代入された値をデータベースに新規登録

### redirect()->route()は何をしているか

一覧画面へのリダイレクト

## その他

### テーブル構成をマイグレーションファイルで管理するメリット

作業者全員でデータベースの状態を共有できる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか

up = artisan
down = rollback

### Seederクラスの役割は何か

データベースに登録する内容を記述

### route関数の引数・返り値・使用するメリット

引数 = ルート名
返り値 = ルート名を元にしたURL
メリット = 記述の簡略化と修正時の利便性

### @extends・@section・@yieldの関係性とbladeを分割するメリット

@extends = 子Bladeに継承する親bladeファイルの指定
@section = 親bladeg側に読み込む子Bladeファイル内の部分指定
@yield = 親bladeg側に子Bladeファイルの一部を読み込む場所指定
メリット = 共通する部分の記述修正がある場合に1ファイルの修正だけで完了できる

### @csrfは何のための記述か

CSRF対策の実装

### {{ }}とは何の省略系か

htmlspecialchars関数